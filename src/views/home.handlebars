<h1>Mis Productos</h1>

<div class="containerCards">
  {{#each productos}}
    <div class="card">
      <p>{{this.title}}</p>
      <p>{{this.description}}</p>
      <p>Precio: ${{this.price}}</p>
    </div>
  {{/each}}
</div>